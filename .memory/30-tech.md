# Technology Landscape

## Tooling
- `pnpm` workspaces + `nx` orchestration
- `typescript` (strict)
- `vitest` for tests
- `tsup` for builds (per package)

## Key deps
- `@modelcontextprotocol/sdk`
- `express`
- `zod`

## Standard commands
```bash
pnpm install
pnpm build
pnpm test
pnpm lint
pnpm typecheck
```

