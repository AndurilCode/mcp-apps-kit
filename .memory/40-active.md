# Active Focus

## Current task
- Add a new example in `examples/` for a weather app, scaffolded via `create-app` CLI.
- Include tests written using the monorepo `packages/testing` library.

## Notes
- Example should avoid real network usage in tests; mock weather service responses.

